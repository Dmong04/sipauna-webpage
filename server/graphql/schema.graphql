type User {
    userId: ID!
    fullname: String!
    email: String!
    roleId: Int!
  }

  type AuthPayload {
    token: String!
    user: User!
  }

  type Query {
    users: [User!]!
    user(userId: ID!): User
  }

  type Mutation {
    login(email: String!, password: String!): AuthPayload
  }